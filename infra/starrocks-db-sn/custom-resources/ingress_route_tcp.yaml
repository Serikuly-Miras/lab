apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: starrocks-db-sn-cluster-route
spec:
  entryPoints:
    - starrocks-db-sn-query-entrypoint
  routes:
    - match: HostSNI(`*`)
      services:
        - name: starrocks-db-sn-cluster-fe-service
          port: 9030
