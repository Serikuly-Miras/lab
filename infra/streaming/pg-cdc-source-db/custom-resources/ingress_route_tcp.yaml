apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: pg-cdc-source-db-route
  namespace: pg-cdc-source-db
spec:
  entryPoints:
    - postgres-pg-cdc-source-db
  routes:
    - match: HostSNI(`*`)
      services:
        - name: pg-cdc-source-db-rw
          port: 5432
