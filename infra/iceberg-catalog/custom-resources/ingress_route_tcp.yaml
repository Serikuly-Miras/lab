apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: iceberg-catalog-db-route
  namespace: iceberg-catalog
spec:
  entryPoints:
    - postgres-iceberg-catalog
  routes:
    - match: HostSNI(`*`)
      services:
        - name: iceberg-catalog-cnpg-db-rw
          port: 5432
