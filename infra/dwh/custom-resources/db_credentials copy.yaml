apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: dwh-db-etl-creds-static-secret
  namespace: dwh
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  vaultAuthRef: vault-auth
  mount: kvv2
  type: kv-v2
  path: dwh-db-etl-creds
  destination:
    create: true
    name: dwh-db-etl-credentials
    transformation:
      excludeRaw: true
      includes:
        - username
        - password
  refreshAfter: 10s
