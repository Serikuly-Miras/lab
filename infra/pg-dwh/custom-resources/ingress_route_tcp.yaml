apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: pg-dwh-rw
spec:
  entryPoints:
    - pg-db-entrypoint
  routes:
    - match: HostSNI(`*`)
      services:
        - name: pg-dwh-rw
          port: 5432
