apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: clickhouse-db-route
  namespace: clickhouse
spec:
  entryPoints:
    - clickhouse
  routes:
    - match: HostSNI(`*`)
      services:
        - name: ch-lab-clickhouse-headless
          port: 9000
