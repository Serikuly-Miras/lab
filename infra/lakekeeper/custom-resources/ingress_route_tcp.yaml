apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: lakekeeper-catalog-db-route
spec:
  entryPoints:
    - postgres-lakekeeper-catalog
  routes:
    - match: HostSNI(`*`)
      services:
        - name: lakekeeper-catalog-cnpg-db-rw
          port: 5432
