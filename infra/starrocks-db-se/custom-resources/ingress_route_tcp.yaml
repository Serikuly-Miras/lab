apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: starrocks-db-se-cluster-route
spec:
  entryPoints:
    - starrocks-db-se-query-entrypoint
  routes:
    - match: HostSNI(`*`)
      services:
        - name: starrocks-db-se-cluster-fe-service
          port: 9030
