apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: starrocks-db
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: starrocks-db
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    automated:
      prune: true
      selfHeal: true
  sources:
    - repoURL: http://gitea-http.gitea.svc.cluster.local:3000/miras/lab
      targetRevision: HEAD
      path: infra/starrocks-db/custom-resources
