master:
  enabled: true
  replicas: 1
  affinity: ""
  data:
    type: "persistentVolumeClaim"
    size: "5Gi"
    storageClass: "local-path"

  logs:
    type: "persistentVolumeClaim"
    size: "5Gi"
    storageClass: "local-path"

volume:
  enabled: true
  replicas: 1
  affinity: ""

  dataDirs:
    - name: data1
      type: "persistentVolumeClaim"
      size: "150Gi"
      storageClass: "local-path"
      maxVolumes: 0 # If set to zero, the limit will be auto configured

filer:
  enabled: true
  replicas: 1
  storage: 5Gi
  storageClass: "local-path"

  data:
    type: "persistentVolumeClaim"
    size: "5Gi"
    storageClass: "local-path"

  logs:
    type: "persistentVolumeClaim"
    size: "5Gi"
    storageClass: "local-path"

  s3:
    enabled: true
    enableAuth: true
    existingConfigSecret: "sw-s3-auth-secret"

s3:
  enabled: true
  replicas: 1
  enableAuth: true
  existingConfigSecret: "sw-s3-auth-secret"

  logs:
    type: "persistentVolumeClaim"
    size: "5Gi"
    storageClass: "local-path"
