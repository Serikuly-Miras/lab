- name: Update apt cache and upgrade packages
  hosts: k3s_cluster
  become: yes
  tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: yes
    - name: Upgrade all packages
      ansible.builtin.apt:
        upgrade: yes
